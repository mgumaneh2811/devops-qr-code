annotated-types==0.9.0          # newer version
anyio==3.9.2                   # newer version
boto3==1.32.45                 # newer version (ensure matches botocore)
botocore==1.32.45              # align with boto3
click==8.1.7                   # keep same — still recent and compatible
exceptiongroup==1.2.0          # keep same — compatible
fastapi==0.109.1               # newer version of FastAPI
h11==0.15.0                    # newer version
idna==3.6                      # keep same — latest stable
jmespath==1.0.1                # keep same — still fine
Pillow==10.4.0                 # newer version of Pillow
pydantic==2.9.0                # newer major for Pydantic v2
pydantic_core==2.15.0          # newer version aligning with pydantic
pypng==0.20230717.0            # slightly newer
python-dateutil==2.8.2         # keep same — still fine
python-dotenv==1.0.0           # keep same — still fine
qrcode==8.1.0                  # newer version of qrcode
s3transfer==0.13.0             # newer version
six==1.16.0                    # keep same — still fine
sniffio==1.4.0                 # newer version
starlette==0.28.1              # newer version (Starlette underlying FastAPI)
typing_extensions==4.9.0       # keep same — still fine
urllib3==1.26.19               # newer patch version
uvicorn==0.27.0                # newer version of Uvicorn
